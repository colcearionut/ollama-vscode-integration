{
  "name": "ollama-vscode-integration",
  "displayName": "ollama-vscode-integration",
  "description": "Extension for VSCode to prompt local Ollama models like DeepseekR1",
  "version": "0.0.1",
  "main": "./out/extension.js",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "scripts": {
    "start": "code --extensionDevelopmentPath=./src",
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.17.17",
    "@types/vscode": "^1.60.0",
    "typescript": "^4.5.2",
    "vsce": "^1.23.0"
  },
  "typescript": {
    "tsconfig": "./tsconfig.json"
  },
  "contributes": {
    "commands": [
      {
        "command": "ollama.prompt",
        "title": "Prompt a local Ollama instance."
      }
    ],
    "keybindings": [
      {
        "command": "ollama.prompt",
        "key": "alt+shift+z",
        "when": "editorTextFocus"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "ollama.prompt",
          "group": "navigation"
        }
      ]
    }
  },
  "dependencies": {
    "ollama-node": "^0.1.28"
  }
}
